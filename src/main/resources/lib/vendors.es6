// import * as moment from 'moment'
const moment = require('moment/min/moment-with-locales')

const numeral = require( 'numeral')

log.info('WWEEEEndors')
log.info('%s', JSON.stringify(JSONstat, null, 2))

export default {
  moment,
  JSONstat,
  numeral
}
